{
  "name": "adv-engaged-citizenship-lite",
  "version": "1.0.0",
  "scripts": {
    "start": "node server",
    "server": "nodemon server.js --ignore client",
    "client": "cd client && npm start",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "concurrently": "^6.2.1",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "pg": "^8.7.1",
    "prettier": "^2.4.1",
    "react-router-dom": "^5.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
